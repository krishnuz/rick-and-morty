import { colors } from '../../../styles/constants'

const styles = {
  wrapper: {
    display: 'flex',
    flexWrap: 'wrap',
    background: colors.black,
    padding: '12px 12px 0',
  },
}

export default styles
